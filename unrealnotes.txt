like the ease/speed of getting into a playable proj compared to unity, create third person demo and you can run around already and it all just works, 3rd person controller and spawn point already created as actors
also the editor is nicer, both aesthetically and the controls/navigation ability of the mouse, cam speed on scroll wheel and all the camera move controls are really nice and slick
actor selection tools top left, e to switch to rotate, r for scale, the gizmos are nicer, size/pos/rot scale options also in top left! very nice

alt key and drag for instant duplication

geomtry bush is awesome ? additive and subtractivem builds levels so quick with 3d actors, even stairs!

live coding is good but if many changes to header file probbaly recompile from vs, also when starting a new session always refresh from vs, seems slightly slow?

same variables as c# except FString for unreal strings as basic c++ is char

when using ue log with an f string you need to give it an asterix 

funny comment - capitilized variables name is sickening ! e.g. Tick(float DeltaTime) not a fan deltaTime is much nicer

macros vs functions ? 
n Unity, you write Debug.Log($"Player health is {hp}");. The downside is that even if you disable logging in your release build, C# still has to format that string,
 allocate memory for it, and call the function (which then immediately returns).

Treat macros as code generators, not logic executors.

    Use Functions for game logic, math, and anything that needs to happen while the game is running.

    Use Macros (or rather, use Unreal's built-in macros) when you need to strip code out of final builds (like debug lines), 
    handle cross-platform boilerplate, or do things that require information about the source file itself.

    Unreal C++ Macros vs. Functions

    The Core Difference: Macros are literal copy-paste text replacements that happen before the code even compiles. Functions are compiled blocks of logic that execute at runtime.

    Type Safety: Functions are strictly type-checked (just like your C# methods). Macros completely bypass type checking because they just blindly paste text.

    Debugging: You can step into a function line-by-line in Visual Studio or Rider. You cannot step into a macro.

    Performance: Macros have zero function-call overhead (because the code is injected directly into the line), but heavy use can bloat your compiled file size.

    Unity/C# Equivalent: There isn't one. C# #define is only for conditional compilation (#if UNITY_EDITOR), whereas Unreal C++ macros (UE_LOG, TEXT()) are literally writing or formatting code for you behind the scenes before the compiler steps in.



member variables go in the header file
in unreal you can use UPROPERTY Macro above it with (EDITANYWHERE) argument, this lets us edit the variable in the unreal engine inspector, VisibleAnywhere is similar but just makes it viewable
in the hiearchy

similar to serialize field private in unity c# for editanywhere but private in script, 

first component added to an actor becomes root component

tick unreals version of unitys update, begin play = start

eject from game is cool, lets you breaK from the player controller and fly around whilst the game is running - very nice

blueprint child classes of our classes let us add basic behaviour to stop us repeating having to add components to an instance of our class, seems really good for
creating the visual assets of an actor

:: scope resolution operator access class functions, . operator access instance functions

assets used from fab library, fab library is really great
Construction Site VOL. 1 - Supply and Material Props https://www.fab.com/listings/ba44a508-bfa5-444c-bbf4-69e8b5dee530
Survival Character Free - https://www.fab.com/listings/11d20d01-b764-4936-8163-cb20d05c369e
Stylized Eastern Village - https://www.fab.com/listings/9841fee2-683f-4e68-adb8-bafec270a251

"play from here" feature is awesome, really nice quality of life
